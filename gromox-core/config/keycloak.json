{
  "realm": "${KEYCLOAK_REALM}",
  "auth-server-url": "${KEYCLOAK_URL}",
  "resource": "${KEYCLOAK_CLIENT_ID}",
  "credentials": {
    "secret": "${KEYCLOAK_CLIENT_SECRET}"
  },
  "confidential-port": 0,
  "realm-public-key": "${BEARER_PUBKEY}"
}
